# Format and lint staged files with Biome
npx biome check --write $(git diff --cached --name-only --diff-filter=ACM -- '*.ts' '*.tsx' '*.js' '*.jsx' '*.json')

# Run full validation
npm run pre-commit
